The test-replace.scm file is a program that runs 21 different tests to verify the accuracy of the replace method.
The replace.scm file contains the replace method and can be used for manual testing.
The replace method uses a copy method during search/replace operation rather than editing the original source.